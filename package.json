{
  "name": "server",
  "version": "1.0.0",
  "description": "Express Server to run all applications under a single domain",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Christian Budhi",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.0.8",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "mocha": "^7.1.1",
    "supertest": "^4.0.2"
  }
}